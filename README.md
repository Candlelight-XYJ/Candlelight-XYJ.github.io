# Blog Source Workflow

This repository folder is the Hexo source workspace.

- Local preview: `npm run server`
- Local build: `npm run build`
- Deploy generated site to GitHub Pages: `npm run deploy`

Recommended branch workflow:
- `source`: Hexo source files
- `master`: generated static site for GitHub Pages